export interface SignPlanetConnection {
  name: string,
  planet: number,
  sign: number,
  id: number,
  description: string // HTML разметка
}

export interface PlanetHouseConnection {
  name: string,
  planet: number,
  house: number,
  id: number,
  description: string // HTML разметка
}

export interface MasterHouseConnection {
  name: string,
  planet: number,
  house: number,
  id: number,
  description: string // HTML разметка
}
